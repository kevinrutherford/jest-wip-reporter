describe('addToReport', () => {
  describe('given a test with an ancestor', () => {
    it.todo('adds a suite whose name is that of the ancestor')

    it.todo('adds the test as a child of the suite')
  })
})
